// ? Lets see the following variable conversion to Number and their results

let a = "Priyam" // * NaN
let b = "22"    // * 22
let c = "Priyam22" // *NaN
let d = null    // * 0
let e = true    // * 1

console.log(Number(d))// * Conversion

//!____________________________________________________________________________

// ? Lets see the following variable conversion to String and their results

let f = 22   // * 22
let g = null    // * null
let h = true    // * true

console.log(String(h))